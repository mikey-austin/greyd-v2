# doc/greyd.conf.5.md, DATABASE SECTION: driver example ("..." removed).
section database {
    driver = "bolt"
    #driver = "sqlite"
    #driver = "mysql"
    #driver = "postgresql"
    #driver = "memory"

    # Driver-specific options below.
}
