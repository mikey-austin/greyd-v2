# Pre-Go greyd.conf files could carry low_prio_mx in the default section
# (the C code read it from there). The loader still accepts it and warns
# that it belongs in the grey section.
low_prio_mx = "10.10.20.30"

section grey {
    enable = 1
}
