# doc/greyd.conf.5.md, DESCRIPTION: section syntax example. The section
# name is not one the loader knows, so it is carried through untouched.
section sectionname {
    var1 = "val1"
    var2 = 10
    var3 = [ 1, 2, 3 ]
}
