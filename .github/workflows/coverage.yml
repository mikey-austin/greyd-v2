name: Coverage

# Unit test coverage with a minimum total; "make coverage" runs the same
# check locally (packages/release.mk).

on: [push, pull_request]

permissions:
  contents: read

env:
  CGO_ENABLED: '0'
  COVER_MIN: '70'

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      # -coverpkg attributes coverage to the package a statement lives in
      # rather than to the package whose tests ran, so shared code such as
      # the SQL adapter base counts. The generated ANTLR parser and the
      # conformance test helpers are left out of the total.
      - name: test with coverage
        run: |
          go test -coverprofile=cover.raw -covermode=atomic -coverpkg=./... ./...
          grep -v -e '/internal/config/grammar/' -e '/adapters/db/dbtest/' cover.raw > cover.out

      - name: report
        run: |
          go tool cover -func=cover.out | tail -1
          go tool cover -html=cover.out -o coverage.html
          {
            echo '### Coverage'
            echo
            echo '```'
            go tool cover -func=cover.out | tail -1
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: |
            cover.out
            coverage.html
          if-no-files-found: error

      - name: enforce minimum
        run: |
          total=$(go tool cover -func=cover.out | tail -1 | awk '{ print $NF }' | tr -d '%')
          echo "total coverage: ${total}% (minimum ${COVER_MIN}%)"
          awk -v t="$total" -v m="$COVER_MIN" 'BEGIN {
              if (t + 0 < m + 0) {
                  printf "coverage %.1f%% is below the %d%% minimum\n", t, m
                  exit 1
              }
          }'
